#include "main.h"

/**
 * _islower - short description, single line
 * @c: contains value to be compared
 * Return: always 0.
 */
int _islower(int c)

{
	if (c > 'a' && c < '2')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
