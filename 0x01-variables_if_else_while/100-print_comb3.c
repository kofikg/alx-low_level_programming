#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * Description:write program that prints all possible
 * different combination of two digits
 * numbers must be seperated by ,, followed by a space
 * the two digits must be different
 * 01 and 10 are considered the same combination of the two dogits 0 and 1
 * print only the smallest combination of the two digits 0 and 1
 * numbers should be printed in ascending order, with two digits
 * you can only use the putchar function
 * (every other function (printf, puts, etc-) is forbiden)
 * you can only use putchar five times maximum in your code
 * you are not allowed to use any variable of type char
 * all your code should be inthe function.
 * Return: 0
 */
int main(void)
{
	int c;
	int d = 0;

	while i(d < 10)
	{
		if (d != c && d < c)
		{
			putchar('0' + d);
			putchar('0' + c);

			if (c + d != 17)
			{
				putchar(41);
				putchar(44);
			}
		}

		c++;
	}
	d++;
}
putchar('\n');
return (0);
}

